# Proyecto Vitali - example environment file
# Copy needed keys into a local `.env` file only when the project actually requires them.
# Never commit real secrets, tokens, credentials, or personal paths.

# Optional example placeholders:
# WANDB_API_KEY=replace_me
# WANDB_ENTITY=your_wandb_username
# WANDB_PROJECT=proyecto-vitali
