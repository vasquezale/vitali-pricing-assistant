"""Explicit contracts for stable paths and artifacts.

These modules do not introduce new behavior; they codify the canonical names and
relative locations already used by the pipeline (see vault docs/architecture/*).
"""

